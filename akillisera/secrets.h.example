// secrets.h.example — ŞABLON.
// Kopyala:  cp secrets.h.example secrets.h  ve kendi değerlerini gir.
// secrets.h .gitignore içindedir, repoya yüklenmez. Bu şablon repoda kalır.
// Yalnızca ihtiyaç duyan modüller include eder (network.h, cloud.h). config.h ETMEZ.
#pragma once

// Wi-Fi (Faz 6)
constexpr char WIFI_SSID[]     = "AGINIZIN_ADI";
constexpr char WIFI_PASSWORD[] = "AG_PAROLASI";

// ThingSpeak (Faz 8)
constexpr char TS_WRITE_API_KEY[] = "THINGSPEAK_WRITE_API_KEY";
constexpr char TS_CHANNEL_ID[]    = "THINGSPEAK_CHANNEL_ID";

// TalkBack (Faz 8)
constexpr char TB_ID[]  = "TALKBACK_ID";
constexpr char TB_KEY[] = "TALKBACK_API_KEY";
